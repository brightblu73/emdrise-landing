Replace the current endorsements/ticker section with a static horizontal band that feels authoritative (like the “recognised by” blocks you see on other EMDR pages), but using EMDRise branding and NAMES ONLY (no logos, no links).

GOALS
- Full-width gradient section using EMDRise blue→green.
- White-out, uppercase organisation names in a single horizontal band with subtle separators.
- Static (no scrolling), responsive, accessible, and visually balanced.
- No external links.

ORGANISATIONS (exact text, no logos):
NICE
World Health Organization
American Psychological Association
U.S. Department of Veterans Affairs
EMDR International Association (EMDRIA)
SAMHSA

IMPLEMENTATION

1) Remove the existing endorsements ticker markup and any related CSS/JS.

2) In index.html, add this section (place it where the old endorsements lived):

<section class="recognitions recognitions--band" aria-labelledby="recog-heading">
  <div class="container">
    <h3 id="recog-heading">EMDR therapy is recognised or recommended by</h3>
    <ul class="band" role="list" aria-label="Recognising organisations">
      <li class="name">NICE</li>
      <li class="name">World Health Organization</li>
      <li class="name">American Psychological Association</li>
      <li class="name">U.S. Department of Veterans Affairs</li>
      <li class="name">EMDR International Association (EMDRIA)</li>
      <li class="name">SAMHSA</li>
    </ul>
    <p class="caption">
      These organisations recognise EMDR therapy in clinical guidelines. Inclusion here reflects support for EMDR as a method, not this specific app.
    </p>
  </div>
</section>

3) In styles.css, append these styles (use brand tokens already defined; do not alter fonts or other branding):

/* Recognitions: static horizontal band */
.recognitions {
  background: linear-gradient(135deg, var(--primary-blue) 0%, #28b2ff 45%, var(--primary-green) 100%);
  color: #fff;
  padding: clamp(48px, 6vw, 80px) 16px;
}
.recognitions .container {
  max-width: var(--maxw);
  margin: 0 auto;
}
.recognitions h3 {
  color: #fff;
  text-align: center;
  font-weight: 800;
  letter-spacing: 0.03em;
  font-size: clamp(1.1rem, 2.2vw, 1.375rem);
  margin: 0 0 24px;
}

/* The band: single row on large screens; wraps cleanly on small screens */
.recognitions .band {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-wrap: wrap; /* allow wrap on small screens */
  gap: 0;          /* we’ll use dividers instead of gaps on wide screens */
  background: rgba(255,255,255,0.06);
  backdrop-filter: saturate(140%) blur(2px);
  border-radius: 14px;
  padding: 10px 12px;
  margin-inline: auto;
  max-width: 1100px;
}

/* Name items */
.recognitions .band .name {
  list-style: none;
  color: #fff;
  font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  white-space: nowrap;
  padding: 10px 22px;
  font-size: clamp(0.8rem, 1.4vw, 1rem);
  text-align: center;
  position: relative;
}

/* Vertical divider between items (hidden on first item) */
.recognitions .band .name + .name::before {
  content: "";
  position: absolute;
  left: 0;
  top: 50%;
  width: 1px;
  height: 18px;
  background: rgba(255,255,255,0.4);
  transform: translateY(-50%);
}

/* On small screens, drop the dividers so wrapped lines don’t look broken */
@media (max-width: 900px) {
  .recognitions .band {
    gap: 8px 12px;              /* spacing between chips when wrapping */
    padding: 12px 10px;
  }
  .recognitions .band .name + .name::before {
    display: none;
  }
}

/* Caption */
.recognitions .caption {
  color: rgba(255,255,255,0.88);
  text-align: center;
  margin: 16px auto 0;
  font-size: clamp(0.9rem, 1.3vw, 1rem);
  max-width: 800px;
}

/* Respect reduced motion preferences (no animations are used, but keep block here for consistency) */
@media (prefers-reduced-motion: reduce) {
  /* no-op */
}

4) Do not introduce logos or external links. Keep names only.

5) Ensure the section is accessible: semantic heading, role="list", strong contrast, readable sizes.

6) Commit with:
"feat(recognitions): static horizontal band with white-out organisation names (no logos)"